<div class= "container-fluid">

  <h3>Please pay the amount shown for a <%if current_user.group.full_share?%>full share<%elsif current_user.group.two_thirds_share?%>two thirds share<% end %> lease.  You may begin making your reservations once payment has been submitted.</h3>

<%= form_tag charges_path do %>
  <article>
    <label class="amount">
      <span>Amount: $<%= @amount %>.00</span>
    </label>
  </article>

  <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
          data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
          data-description="A month's subscription"
          data-amount="<%= @amount * 100 %>"></script>
<% end %>
<h1><%= current_user.group.boat.name%></h1>
<img src="<%=current_user.group.boat.image_url%>" width="250" >
</div>